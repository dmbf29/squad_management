<%= simple_form_for [team, squad] do |f| %>
  <%= f.input :name, placeholder: 'u23s' %>
  <%= f.input :total_rows, as: :hidden, input_html: { value: 1 } %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
